spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zentra?useSSL=false&serverTimezone=UTC
    username: root
    password: Ajiang123,.

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms

  kafka:
    bootstrap-servers: localhost:9094
    consumer:
      group-id: zentra-default