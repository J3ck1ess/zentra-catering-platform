# Production profile configuration
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/zentra?useSSL=false&serverTimezone=UTC
    username: root
    password: Ajiang123,.

  data:
    redis:
      host: redis
      port: 6379
      database: 0
      timeout: 3000ms