<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zentra.server.mapper.OrderItemMapper">

    <!-- ========================================= -->
    <!-- Result Mapping -->
    <!-- ========================================= -->
    <resultMap id="OrderItemResultMap"
               type="com.zentra.server.entity.OrderItem">

        <id property="id" column="id"/>

        <result property="merchantId" column="merchant_id"/>
        <result property="orderId" column="order_id"/>
        <result property="dishId" column="dish_id"/>
        <result property="dishName" column="dish_name"/>
        <result property="price" column="price"/>
        <result property="quantity" column="quantity"/>
        <result property="amount" column="amount"/>

    </resultMap>

    <!-- ========================================= -->
    <!-- Insert order item -->
    <!-- ========================================= -->
    <insert id="insert"
            parameterType="com.zentra.server.entity.OrderItem"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO order_item (
            merchant_id,
            order_id,
            dish_id,
            dish_name,
            price,
            quantity,
            amount
        )
        VALUES (
                   #{merchantId},
                   #{orderId},
                   #{dishId},
                   #{dishName},
                   #{price},
                   #{quantity},
                   #{amount}
               )

    </insert>

    <!-- ========================================= -->
    <!-- Query order items by order id -->
    <!-- ========================================= -->
    <select id="findByOrderId"
            resultMap="OrderItemResultMap">

        SELECT
            id,
            merchant_id,
            order_id,
            dish_id,
            dish_name,
            price,
            quantity,
            amount
        FROM order_item
        WHERE order_id = #{orderId}
        AND merchant_id = #{merchantId}

    </select>

</mapper>