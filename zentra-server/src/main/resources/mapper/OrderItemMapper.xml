<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zentra.server.mapper.OrderItemMapper">

    <insert id="insert"
            parameterType="com.zentra.server.entity.OrderItem">

        INSERT INTO order_item
            (order_id, dish_id, dish_name, price, quantity, amount)
        VALUES
            (#{orderId}, #{dishId}, #{dishName}, #{price}, #{quantity}, #{amount})

    </insert>

</mapper>