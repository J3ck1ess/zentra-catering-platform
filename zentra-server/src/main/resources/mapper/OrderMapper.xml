<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zentra.server.mapper.OrderMapper">

    <insert id="insert"
            parameterType="com.zentra.server.entity.Order"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO orders
            (merchant_id, order_number, total_amount, status)
        VALUES
            (#{merchantId}, #{orderNumber}, #{totalAmount}, #{status})

    </insert>

    <select id="findPage" resultType="com.zentra.server.entity.Order">

        SELECT
            o.id,
            o.order_number,
            o.total_amount,
            o.status,
            o.created_at
        FROM orders o
        WHERE o.merchant_id = #{merchantId}

        <if test="status != null">
            AND o.status = #{status}
        </if>

        ORDER BY o.created_at DESC
        LIMIT #{offset}, #{pageSize}

    </select>

    <select id="count" resultType="java.lang.Long">

        SELECT COUNT(*)
        FROM orders
        WHERE merchant_id = #{merchantId}

        <if test="status != null">
            AND status = #{status}
        </if>

    </select>

    <select id="findById" resultType="com.zentra.server.entity.Order">
        SELECT
            o.id,
            o.order_number,
            o.total_amount,
            o.status,
            o.created_at
        FROM orders o
        WHERE o.id = #{id}
        AND o.merchant_id = #{merchantId}
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status}
        WHERE id = #{id}
        AND merchant_id = #{merchantId}
    </update>

</mapper>