spring:
  datasource:
    url: jdbc:mysql://localhost:3307/zentra_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Ajiang123,.

  data:
    redis:
      host: localhost
      port: 6380
      database: 1
      timeout: 3000ms

  jackson:
    default-property-inclusion: non_null

mybatis:
  mapper-locations: classpath:mapper/*.xml

  configuration:
    map-underscore-to-camel-case: true